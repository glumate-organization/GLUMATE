# 글루메이트 기업 사이트 — Claude Code 가이드

글루메이트(GLUMATE) 회사 웹사이트. Astro 정적 사이트 → GitHub Pages(glumate.org).
제품 사이트(글루밍, gluming.app)는 `../Gluming/docs`에 따로 있다. **이 리포에서 그쪽을 건드리지 않는다.**

전체 구조·명령어·모션 API는 `README.md`에 있다. 이 파일은 작업할 때의 판단 기준만 담는다.

---

## 0. 항상 지키는 4가지

1. **자가완결**: 런타임 외부 의존 0. 이미지·폰트는 `src/assets/` 번들, 외부 CDN·이미지 URL 금지.
   API 호출은 빌드 시점에만 (`src/lib/business.ts` 패턴).
2. **웰니스 경계**: 의료기기·진단·치료 서비스가 아니다. "치료/완치/정상화/진단/처방",
   효능·수치 보장 표현 금지. → "생활습관 관리를 돕는", "미리 살펴보는", "참고할 수 있는".
3. **확인 가능한 사실만**: 특허는 **출원(심사 진행 중)**. 등록된 권리처럼 쓰지 않는다.
   근거 없는 수치·통계·효과를 카피에 넣지 않는다.
4. **등록 정보는 푸터에만**: 사업자등록번호·통신판매업 신고번호·조회 링크는 `Footer.astro`에만 둔다.
   본문에 "사업자 정보 확인하세요" 류의 섹션을 만들지 않는다. (정훈님 명시 요청)

## 1. 이 사이트가 보여줘야 하는 것

우선순위 그대로다. 새 섹션을 만들 때 이 셋 중 무엇을 강화하는지 먼저 답할 것.

1. **기술적으로 뛰어난 회사** — 특허 출원 3건, 데이터 파이프라인
2. **㈜아이센스 파트너십** — 케어센스 에어 제조사와의 개발 파트너십 (CGM 데이터 정식 연동)
3. **청년창업사관학교 16기 선정** — 중소벤처기업진흥공단

특허는 **자세히 적지 않는다.** 출원번호·청구범위·명세서 내용은 공개하지 않고, 기술 영역만 한 줄로.
적을 내용이 없으면 억지로 늘리지 말고 비워 둔다.

## 2. 내용은 `src/lib/`에서 고친다

| 파일 | 담는 것 |
| --- | --- |
| `company.ts` | 상호·대표·사업자등록번호·통신판매업 신고번호·주소 (증빙 서류 기재값 그대로) |
| `credentials.ts` | 특허 출원 영역 3건, 파트너십·선정 이력 |
| `assets.ts` | 로고·제품 이미지, 제품 목록 (`products` 배열) |
| `links.ts` | 외부 링크 전부 |

페이지에 값을 하드코딩하지 않는다. 제품이 늘면 `assets.ts`의 `products` 배열에 추가한다.

**절대 넣지 않는 것**: 대표자 생년월일·주민등록번호, 특허 출원번호, 파트너십 계약의 상업적 조건
(공급가·수수료 등), API 키.

## 3. 디자인 시스템

- 브랜드색 `--navy: #004294` — 로고 원본 PNG에서 직접 샘플링한 값이다. 임의로 바꾸지 않는다.
- 색·간격·반경·글자크기는 `global.css`의 CSS 변수만 쓴다. **하드코딩 금지**, 새 `clamp()` 만들지 말 것.
- 컨셉은 "설계도(blueprint)": 얇은 헤어라인, 규칙적 격자(`--grid-light`/`--grid-dark`), 작은 반경, 절제된 그림자.
- 로고는 배경에 맞춰 고른다. 밝은 면 = `logos.horizontal`(네이비), 네이비 면 = `logos.horizontalWhite`.
  **CSS 필터로 로고 색을 뒤집지 않는다.** 화이트 원본이 이미 있다.

## 4. 모션 (`src/scripts/motion.ts`)

마크업 API는 README 표 참고. 새 섹션에 모션을 붙일 때 지킬 것:

- **`data-parallax`와 `.intro`/`[data-reveal]`을 같은 요소에 걸지 않는다.** JS 인라인 `transform`이
  CSS 등장 `transform`을 덮어써서 요소가 영영 안 보인다. 래퍼를 하나 두고 나눠 건다.
  (`index.astro`의 `.plate-wrap` / `.plate` 가 그 예)
- 콘텐츠를 숨기는 CSS는 반드시 `.js` 안에 둔다. JS 없이도 다 보여야 한다.
- rAF 의존 로직에는 `setTimeout` 안전망을 둔다 (백그라운드 탭에서 rAF가 멈춘다).
- `prefers-reduced-motion`을 항상 처리한다.

## 5. 작업 방식

- 변경 전에 **무엇을 / 어느 파일에 / 왜** 를 먼저 말하고 진행한다.
- 변경 후 자가검증: `npm run build` 통과 · 외부 런타임 의존 안 생겼는지 · 금지 표현 없는지.
- `dist/`는 직접 수정하지 않는다 (빌드 산출물).
- 보호 경로 — 수정 전 확인: `astro.config.mjs`, `package.json`, `.github/workflows/**`,
  `src/lib/company.ts`(법적 기재사항), `public/CNAME`.

## 6. 내부 링크는 반드시 url()

지금 사이트는 `github.io/GLUMATE/` 하위 경로로 서빙된다. `href="/about"` 처럼 하드코딩하면
CSS·이미지·링크가 전부 404 난다. **모든 내부 경로는 `src/lib/url.ts` 의 `url()` 을 거친다.**

```astro
import { url } from '../lib/url';
<a href={url('/technology')}>기술</a>
```

커스텀 도메인(glumate.org)으로 옮기는 절차는 README 「배포」 참고.

## 7. 알려진 상태

- 국세청 조회 API(odcloud)가 간헐적으로 503을 낸다. 실패하면 `{ok:false}` 폴백 → 배지만 빠지고
  빌드는 정상. 이건 버그가 아니다.
- 통신판매업 신고번호는 대덕구 이전 기준 `제 2026-대전대덕-0372 호`다.
  `Gluming/docs`의 푸터에는 이전 주소 기준 번호(대전유성-1139)가 남아 있어 서로 다르다.
